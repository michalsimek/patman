version: 2
updates:
  # Keep the GitHub Actions used by the workflows up to date
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly

  # Keep the Python dependencies up to date
  - package-ecosystem: pip
    directory: "/"
    schedule:
      interval: weekly
